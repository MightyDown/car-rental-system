# 题目08：租车管理系统

## 技术栈

- 后端：Spring Boot 3.x
- 持久层：MyBatis 或 Spring Data JPA（任选其一）
- 前端：Vue 3（Composition API）+ Vite + Element Plus
- HTTP客户端：Axios
- 路由：Vue Router 4
- 数据库：MySQL
- 认证：JWT Token

---

## 一、用户故事

### 1.1 故事背景

周经理是某租车公司的运营负责人，最近遇到了几个问题：

**问题一：超时费如何计算？**

客户刘先生租了一辆凯美瑞，计划租用3天（周五09:00取，周一09:00还）。但他周一11:00才来还车，超时了2小时。

系统需要计算超时费：
- 经济型超时费率：20元/小时
- 豪华型超时费率：50元/小时

刘先生问："我超了2小时，是不是收40元？"

周经理说："对，但还有一个规则：**超时小时数向上取整**。超5分钟算1小时，超61分钟算2小时。"

**问题二：时间冲突如何判断？**

两个客户同时预订同一辆车的同一时间段。后提交的预订会和前一个产生时间冲突。

系统必须正确检测**租车时间重叠**。特别注意：**等号算冲突**（因为还车和取车之间需要清洁和交接时间）。

示例：
- A预订周四到周六
- B预订周六到周一
- 周六重叠 → 算冲突，B被拒绝

**问题三：信用分扣到负数怎么办？**

张小姐租车后发生了事故，扣了20分。她的信用分原本是75，扣完后变成55。

系统应该：信用分最低为0，不能变成负数。

这个系统要解决的核心问题：
- **超时小时向上取整**：超时5分钟也算1小时
- **租车时间冲突判断**：等号也算冲突
- **信用分最低为0**：扣分不能导致负数
- **里程免费额按天数叠加**：总免费额 = 天数 × 每天免费里程

---

### 1.2 角色说明

| 角色 | 说明 | 主要操作 |
|------|------|----------|
| 客户 | 租车的用户 | 预订车辆、查看预订状态、查看费用明细 |
| 工作人员（管理员） | 租车公司工作人员 | 确认预订、办理取还车、录入事故、管理车辆 |

---

### 1.3 核心场景

**场景一：客户注册与登录**

新客户注册时需要填写：用户名、密码、驾照号（**必填，用于租车资格验证**）。工作人员审核通过后账号生效。

**示例数据：**
- 客户刘先生：用户名 `li_customer`，驾照号 `110101199001011234`，信用分 100
- 客户张小姐：用户名 `zhang_customer`，驾照号 `110101199502025678`，信用分 75
- 车辆配置：凯美瑞（经济型）日租金200元/天，每小时超时费20元，每天免费里程200公里，每公里1元

---

**场景二：客户预订车辆**

刘先生想租一辆凯美瑞（经济型），计划周六取车、租3天（计划还车时间：周二09:00）。

**预订前置检查：**

| # | 检查项 | 规则 |
|---|--------|------|
| 1 | 车辆可用 | 车辆状态必须是"空闲" |
| 2 | 时间无冲突 | 计划取车日期到计划还车日期之间，该车辆不能有其他进行中的预订 |
| 3 | 无重复预订 | 同一用户不能同时有超过1个进行中的预订 |
| 4 | 账号正常 | 用户账号未被冻结 |
| 5 | 有效驾照 | 驾照号不能为空 |
| 6 | 信用分合规 | 信用分必须 ≥ 60（低于60禁止租车） |

**时间冲突判断规则（等号算冲突）：**

新预订时间段 `[new_start, new_end]` 与已有预订 `[exist_start, exist_end]` 冲突，当且仅当：

```
new_start <= exist_end  AND  exist_start <= new_end
```

**示例分析：**

| 已有预订 | 新申请 | 判断 | 说明 |
|----------|--------|------|------|
| 周四-周六 | 周六-周一 | 冲突 | 周六重叠（等号），需要交接时间 |
| 周四-周六 | 周日-周二 | 冲突 | 周日-周六重叠 |
| 周四-周六 | 周一-周三 | 不冲突 | 周一在周六之后，无重叠 |

> **为什么等号算冲突？**
> - A预订周四还车，周六才能取车
> - B预订周六取车，中间需要清洁和交接时间
> - 因此"等号"也算重叠，不允许同时预订

**预订成功后的操作：**
- 预订状态为"**待确认**"，等待工作人员确认
- 系统计算**预计租金快照** = 计划天数 × 车辆日租金
- 车辆状态变为"**已预约**"

**工作人员确认预订后：**
- 车辆状态变为"**已预留**"
- 预订状态变为"**已确认**"

**数据例子：**

| 车辆 | 已有预订 | 刘先生申请 | 判断 | 结果 |
|------|----------|-----------|------|------|
| 凯美瑞 | 3.6(周四)-3.8(周六) | 3.8(周六)-3.11(周二) | 冲突（周六重叠） | ❌ 拒绝 |
| 凯美瑞 | 无 | 3.8(周六)-3.11(周二) | 无冲突 | ✅ 预订成功 |
| 凯美瑞 | 3.10(周一)-3.12(周三) | 3.8(周六)-3.11(周二) | 冲突（周一重叠） | ❌ 拒绝 |

---

**场景三：取车**

周六早上9:00，刘先生到店取车。工作人员操作：

1. 检查预订状态是否为"**已确认**"
2. 检查车辆状态是否为"**已预留**"
3. 记录**取车时间**（服务器当前时间）
4. 记录**取车里程**（如50000公里）
5. 计算**计划还车时间** = 取车时间 + 计划租用天数
   - 周六09:00 + 3天 = 周二09:00
6. 预订状态变为"**已取车**"，车辆状态变为"**已租用**"

**数据例子：**

| 预订 | 取车时间 | 取车里程 | 计划还车时间 | 预计租金 |
|------|----------|----------|-------------|----------|
| 刘先生凯美瑞3天 | 3月8日09:00 | 50000公里 | 3月11日09:00 | 3×200=600元 |

---

**场景四：还车与费用计算**

刘先生周二上午11:00来还车（超出计划还车时间2小时）。工作人员录入还车里程（如50080公里）。

**全部费用由系统计算，不接受人工输入。**

**1. 超时费用：**

| 项目 | 计算 | 示例 |
|------|------|------|
| 实际还车时间 | 录入 | 周二11:00 |
| 计划还车时间 | 取车时间 + 计划天数 | 周二09:00 |
| 超时分钟数 | 实际 - 计划（向上取整到分钟） | 120分钟 |
| 超时小时数 | ceil(超时分钟数 / 60) | ceil(120/60) = 2小时 |
| 每小时超时费率 | 车辆配置 | 经济型20元/小时 |
| 超时费 | 超时小时数 × 每小时费率 | 2 × 20 = 40元 |

> **向上取整规则（重要）：**
> - 超5分钟 → ceil(5/60) = ceil(0.083) = 1小时
> - 超61分钟 → ceil(61/60) = ceil(1.017) = 2小时
> - **必须用 `Math.ceil()`，不能用四舍五入 `Math.round()`**

**2. 超里程费用：**

| 项目 | 计算 | 示例 |
|------|------|------|
| 每天免费里程 | 车辆配置 | 200公里/天 |
| 总免费里程额 | 计划天数 × 每天免费里程 | 3天 × 200 = 600公里 |
| 取车时里程 | 录入 | 50000公里 |
| 还车时里程 | 录入 | 50080公里 |
| 实际行驶里程 | 还车里程 - 取车里程 | 80公里 |
| 超里程 | max(0, 实际 - 免费额) | max(0, 80-600) = 0公里 |
| 每公里费率 | 车辆配置 | 1元/公里 |
| 超里程费 | 超里程 × 每公里费率 | 0 × 1 = 0元 |

> **免费里程按总天数叠加：** 3天租车的总免费额是600公里（3×200），不是每天单独200公里。

**3. 总费用：**

```
总费用 = 预计租金 + 超时费 + 超里程费
       = (计划天数 × 日租金) + 超时费 + 超里程费
       = (3 × 200) + 40 + 0
       = 640元
```

**还车后的事务操作（必须同一事务）：**
1. 快照值写入预订记录（超时小时数、超时费、超里程费、总费用）
2. 车辆里程更新为还车里程
3. 车辆状态变为"**空闲**"
4. 预订状态变为"**已还车**"

**超时检测与信用分扣减：**

系统检测到预订状态为"**已取车**"且当前时间 > 计划还车时间：
- 状态改为"**超时**"（中间状态）
- 还车时，信用分扣5分（与超时时长无关）
- **信用分最低为0**：公式 `new_score = max(0, old_score - 5)`

**数据例子：**

| 项目 | 数值 | 计算 |
|------|------|------|
| 超时分钟 | 120分钟 | 11:00 - 09:00 |
| 超时小时 | 2小时 | ceil(120/60) |
| 超时费 | 40元 | 2 × 20 |
| 实际里程 | 80公里 | 50080 - 50000 |
| 免费额 | 600公里 | 3 × 200 |
| 超里程 | 0公里 | max(0, 80-600) |
| 超里程费 | 0元 | 0 × 1 |
| 总费用 | 640元 | 600 + 40 + 0 |

---

**场景五：事故处理与车辆维修**

刘先生还车时，工作人员发现车辆有剐蹭，同时提报事故：

1. 创建**事故记录**，关联预订ID和车辆ID
2. 车辆状态变为"**维修中**"，禁止新建预订
3. 管理员评估后填写：
   - 扣分值（如10分）
   - 预计维修结束日期
4. 同时扣除刘先生信用分（扣分值由管理员录入）
5. 维修完成后，填入实际完成日期
6. 车辆状态恢复为"**空闲**"

**信用分最低为0示例：**

| 客户 | 原信用分 | 扣分值 | 计算 | 新信用分 |
|------|----------|--------|------|----------|
| 张小姐 | 75分 | 扣20分 | max(0, 75-20) | 55分 ✅ |
| 王先生 | 15分 | 扣20分 | max(0, 15-20) | 0分（非-5分）✅ |
| 赵女士 | 0分 | 扣5分 | max(0, 0-5) | 0分 ✅ |

---

### 1.4 状态流转说明

**车辆状态流转：**

```
空闲
  ├─→ 已预约（客户预订，待确认）
  │     ├─→ 已预留（工作人员确认预订）
  │     │     └─→ 已租用（客户取车）
  │     │           └─→ 空闲（客户还车）
  │     └─→ 空闲（预订取消）
  └─→ 维修中（发生事故）
        └─→ 空闲（维修完成）
```

**预订状态流转：**

```
待确认
  ├─→ 已确认（工作人员确认）
  │     ├─→ 已取车（客户取车）
  │     │     ├─→ 超时（超过计划还车时间，系统自动标记）
  │     │     │     └─→ 已还车（客户还车，含超时扣分计算）
  │     │     └─→ 已还车（正常还车）
  │     └─→ 已取消
  └─→ 已取消
```

---

### 1.5 边界场景说明

1. **租车时间冲突（等号算冲突）：**
   - A预订周四到周六，B预订周六到周一
   - 周六重叠，算冲突，B被拒绝
   - 判断公式：`A_end >= B_start AND B_end >= A_start`

2. **超时小时向上取整：**
   - 超5分钟 → Math.ceil(5/60) = 1小时
   - 超61分钟 → Math.ceil(61/60) = 2小时
   - 必须用 `Math.ceil()` 而非 `Math.round()`

3. **免费里程额按总天数叠加：**
   - 3天租车，总免费额 = 3 × 200 = 600公里
   - 不是每天单独计算后比较
   - 是汇总后与总行驶里程比较

4. **信用分最低为0：**
   - 任何扣分操作不能使信用分变为负数
   - 公式：`max(0, 信用分 - 扣分值)`
   - 75分扣20分 → max(0, 75-20) = 55分（不是-5分）

5. **还车里程不能低于取车里程：**
   - 录入的还车里程必须 ≥ 取车时的里程记录
   - 否则视为录入错误，系统拒绝

6. **并发预订同一辆车：**
   - 两个用户同时提交相同时间段的预订
   - 后提交的会因车辆状态已变为"已预约"而失败
   - 或使用乐观锁版本号检测

7. **超时费与超时时长无关：**
   - 无论超时5分钟还是超时2小时，只要是超时，扣分都是5分
   - 但超时费是按小时计算的

---

### 1.6 数据库表设计

**用户表**

| 字段 | 说明 |
|------|------|
| 用户编号 | 用户ID，主键自增 |
| 用户名 | 用户名，唯一 |
| 密码 | 密码（加密存储） |
| 真实姓名 | 真实姓名 |
| 驾照号 | 驾照号，唯一 |
| 信用分 | 信用分（默认100） |
| 账号状态 | 状态：normal / suspended |
| 创建时间 | 创建时间（系统自动记录） |

**车辆表**

| 字段 | 说明 |
|------|------|
| 车辆编号 | 车辆ID，主键自增 |
| 车牌号 | 车牌号，唯一 |
| 车型 | 车型名称（如"凯美瑞"） |
| 车辆类型 | 类型：economy / luxury |
| 车辆状态 | 状态：idle / reserved / confirmed / rented / maintenance |
| 当前里程 | 当前里程数（单位：公里） |
| 创建时间 | 创建时间（系统自动记录） |

**车辆配置表**

| 字段 | 说明 |
|------|------|
| 配置编号 | 配置ID，主键自增 |
| 车辆类型 | 类型：economy / luxury |
| 日租金 | 每日租金（单位：元） |
| 每小时超时费 | 每小时超时费率（单位：元/小时） |
| 每天免费里程 | 每天免费里程（单位：公里） |
| 每公里超里程费 | 每公里超里程费率（单位：元/公里） |

**预订记录表**

| 字段 | 说明 |
|------|------|
| 预订编号 | 预订ID，主键自增 |
| 用户编号 | 用户ID，外键 |
| 车辆编号 | 车辆ID，外键 |
| 计划取车时间 | 计划取车时间 |
| 计划还车时间 | 计划还车时间 |
| 计划天数 | 计划租用天数（系统计算） |
| 预计租金快照 | 预计租金（快照） |
| 实际取车时间 | 实际取车时间（取车时记录） |
| 取车里程 | 取车时里程（取车时记录） |
| 实际还车时间 | 实际还车时间（还车时记录） |
| 还车里程 | 还车时里程（还车时记录） |
| 超时分钟数 | 超时分钟数（快照） |
| 超时费 | 超时费（快照） |
| 超里程费 | 超里程费（快照） |
| 总费用 | 总费用（快照） |
| 预订状态 | 状态：pending / confirmed / picked_up / overdue / returned / cancelled |
| 创建时间 | 创建时间（系统自动记录） |

**事故记录表**

| 字段 | 说明 |
|------|------|
| 事故编号 | 事故ID，主键自增 |
| 预订编号 | 预订ID，外键 |
| 车辆编号 | 车辆ID，外键 |
| 事故描述 | 事故描述 |
| 扣分值 | 扣除的信用分值 |
| 预计完成日期 | 预计维修完成日期 |
| 实际完成日期 | 实际维修完成日期（可空） |
| 事故状态 | 状态：pending / in_progress / completed |
| 创建时间 | 创建时间（系统自动记录） |

**ER 关系说明：**

- **一对多（1:N）**：用户 → 预订记录；车辆 → 预订记录；车辆 → 事故记录
- **多对一（N:1）**：预订记录 → 用户；预订记录 → 车辆；事故记录 → 预订记录；事故记录 → 车辆

---

## 二、输入校验汇总

**预订模块：**

| 操作 | 校验字段 | 规则 |
|------|----------|------|
| 预订车辆 | 车辆编号 | 不能为空；车辆必须存在且状态为"空闲" |
| 预订车辆 | 取车日期 | 不能为空；必须是今天或之后 |
| 预订车辆 | 还车日期 | 不能为空；必须晚于取车日期 |
| 预订车辆 | 计划天数 | 不能为空；由系统计算 = 还车日期 - 取车日期 |
| 预订车辆 | （自动检查） | 时间无冲突；无重复预订；账号未冻结；驾照号非空；信用分≥60 |
| 确认预订 | 预订编号 | 不能为空；预订必须存在且状态为"待确认" |
| 取车 | 预订编号 | 不能为空；预订必须存在且状态为"已确认" |
| 取车 | 取车里程 | 不能为空；必须 ≥ 0 |
| 还车 | 还车里程 | 不能为空；必须 ≥ 取车时里程 |

**车辆管理模块：**

| 操作 | 校验字段 | 规则 |
|------|----------|------|
| 新增车辆 | 车牌号 | 不能为空；不能与其他车辆重复 |
| 新增车辆 | 车型 | 不能为空 |
| 新增车辆 | 车辆类型 | 不能为空；只能是"economy"或"luxury" |
| 新增车辆 | 日租金 | 不能为空；必须 > 0 |
| 新增车辆 | 每小时超时费率 | 不能为空；必须 ≥ 0 |
| 新增车辆 | 每天免费里程 | 不能为空；必须 ≥ 0 |
| 新增车辆 | 每公里超里程费 | 不能为空；必须 ≥ 0 |
| 设置维修 | 车辆编号 | 不能为空；车辆必须存在 |
| 维修完成 | 完成日期 | 不能为空；不能早于预计完成日期 |

---

## 三、评分细则

| 评分项 | 分值 | 评分标准 |
|--------|:----:|----------|
| 数据库设计与项目搭建 | 10 | 五张表结构完整、主外键正确、Spring Boot项目启动正常、前后端能联通 |
| 用户认证与JWT实现 | 10 | 客户登录/登出接口可用、JWT Token生成与验证正确 |
| 车辆管理CRUD | 8 | 车辆新增/查询/状态管理功能完整 |
| 预订与取还车流程 | 10 | 预订申请/确认/取车/还车功能完整 |
| **超时小时向上取整** | **12** | ceil()函数正确使用；超时5分钟算1小时；超时61分钟算2小时 |
| **租车时间冲突（等号算冲突）** | **12** | 判断公式正确；等号算冲突；交接时间正确处理 |
| **费用计算（租金+超时+超里程）** | **10** | 预计租金/超时费/超里程费计算正确；免费里程按天数叠加正确；总费用汇总正确 |
| **信用分扣减** | **8** | 每次超时扣5分；信用分最低为0；60分以下冻结账号 |
| Vue前端实现 | 8 | 车辆列表页；预订表单；取还车界面；费用明细展示 |
| **事故与维修管理** | **6** | 事故记录创建；车辆状态联动维修；维修完成后恢复空闲 |
| **合计** | **100** | |

---

## 四、评分说明

本题核心考察点：

1. **超时小时向上取整**：理解 `Math.ceil()` 的作用，以及为什么不能用 `Math.round()`。这是本题的易错点。

2. **租车时间冲突判断**：理解为什么等号也算冲突（需要交接时间），以及如何用数学公式表达这个判断。

3. **免费里程按天数叠加**：理解为什么是总天数乘以每天免费额，而不是每天单独计算。

4. **信用分不能为负数**：理解 `max(0, score - deduction)` 的作用。

---

## 五、提交要求

1. 后端 Spring Boot 项目源码（含 README，说明启动步骤和数据库配置）
2. 前端 Vue 3 项目源码（含 package.json，注明 `npm install` + `npm run dev`）
3. 数据库建表 SQL 脚本（五张表 + 初始化测试数据，含至少2辆车、2个客户账号）
4. 设计文档：超时费用计算说明、时间冲突判断逻辑说明

---

## 六、注意事项

- 超时小时必须向上取整（使用 `Math.ceil()`）
- 租车时间冲突时等号也算冲突
- 免费里程按总天数叠加，不是每天单独计算
- 信用分最低为0，不能扣成负数
- 还车里程不能低于取车里程
- 费用全部由系统计算，不接受人工输入
- 每次超时固定扣5分信用分
- 后端 CORS 配置允许前端开发端口访问
