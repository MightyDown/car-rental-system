# 租车管理系统 - 后端开发规划文档

## 文档说明

本文档为后端开发提供完整的模块分解、开发策略和接口设计规范，供 Claude Code 进行代码生成时参考。

---

## 本地配置

- 本地mysql数据库配置：-u root -p mysql
- 如要使用本地的tomcat和maven路径，就去系统环境变量中去找

## 一、模块划分总览

### 1.1 模块结构图

```
后端系统
├── 核心基础模块
│   ├── 项目配置模块
│   ├── 统一响应模块
│   ├── 异常处理模块
│   └── 工具类模块
│
├── 认证授权模块
│   ├── 用户登录模块
│   ├── JWT生成验证模块
│   └── 权限拦截模块
│
├── 用户管理模块
│   ├── 客户注册模块
│   ├── 客户信息管理
│   └── 信用分管理模块
│
├── 车辆管理模块
│   ├── 车辆CRUD模块
│   ├── 车辆状态管理
│   └── 车辆配置管理
│
├── 预订管理模块（核心）
│   ├── 预订创建模块
│   ├── 时间冲突检测模块 ⭐
│   ├── 预订确认模块
│   └── 预订取消模块
│
├── 取还车模块（核心）
│   ├── 取车处理模块
│   ├── 还车处理模块
│   ├── 费用计算模块 ⭐
│   └── 信用分扣减模块
│
└── 事故管理模块
    ├── 事故登记模块
    ├── 事故处理模块
    └── 维修管理模块
```

### 1.2 模块依赖关系

```
认证授权模块 ←─────┐
     ↓            │
用户管理模块       │ 所有模块都需要认证
     ↓            │
车辆管理模块 ←─────┤
     ↓            │
预订管理模块 ←─────┤
     ↓            │
取还车模块 ←───────┤
     ↓            │
事故管理模块 ←─────┘
```

---

## 二、核心基础模块

### 2.1 项目配置模块

**职责：**
- Spring Boot 应用启动配置
- 多环境配置（dev/prod）
- 跨域配置（CORS）
- MyBatis-Plus 配置

**开发策略：**
- 使用 application.yml 进行多环境配置分离
- CORS 配置允许前端开发端口（如 5173、3000）
- 配置 Jackson 日期格式化统一为 `yyyy-MM-dd HH:mm:ss`
- 开启 MyBatis-Plus 的驼峰命名映射
- 配置数据库连接池（HikariCP）

**关键配置项：**
| 配置项 | 说明 | 示例值 |
|--------|------|--------|
| server.port | 服务端口 | 8080 |
| spring.datasource | 数据库连接 | MySQL 8.0 |
| jwt.secret | JWT密钥 | 自定义密钥串 |
| jwt.expiration | Token有效期 | 86400000（24小时） |
| cors.allowed-origins | 跨域白名单 | http://localhost:5173 |

### 2.2 统一响应模块

**职责：**
- 统一 API 响应格式
- 统一状态码定义
- 分页响应封装

**开发策略：**
- 所有接口返回统一的 Result 对象
- Result 包含：code（状态码）、message（提示信息）、data（业务数据）、timestamp（时间戳）
- 定义常用状态码常量类
- 分页响应使用 PageResult 包装

**响应格式示例：**
- 成功响应：code=200
- 业务失败：code=4xx，携带具体错误信息
- 系统异常：code=500

### 2.3 异常处理模块

**职责：**
- 全局异常捕获
- 业务异常定义
- 参数校验异常处理

**开发策略：**
- 使用 `@RestControllerAdvice` 实现全局异常处理
- 自定义 BusinessException 业务异常类
- 参数校验异常返回 400，包含具体字段错误
- 权限异常返回 401 或 403
- 记录异常日志

**异常类型映射：**
| 异常类型 | HTTP状态码 | 业务码 | 说明 |
|----------|------------|--------|------|
| 参数校验异常 | 400 | 40001 | @Valid 校验失败 |
| 未认证 | 401 | 40100 | Token缺失或无效 |
| 无权限 | 403 | 40300 | 角色权限不足 |
| 资源不存在 | 404 | 40400 | 数据不存在 |
| 业务冲突 | 409 | 40900 | 时间冲突等 |
| 业务规则失败 | 422 | 42200 | 信用分不足等 |
| 系统异常 | 500 | 50000 | 未知错误 |

### 2.4 工具类模块

**职责：**
- JWT 工具类
- 日期时间工具类
- 费用计算工具类（核心）

**开发策略：**
- JwtUtil：生成Token、解析Token、验证Token
- DateUtil：日期计算、相差天数/分钟数、日期比较
- FeeCalculator：超时小时计算（ceil）、超里程计算、总费用计算

---

## 三、认证授权模块

### 3.1 模块职责

- 用户登录认证
- JWT Token 生成与验证
- 请求权限拦截
- 获取当前登录用户信息

### 3.2 开发策略

**认证流程：**
1. 用户提交用户名密码
2. 验证用户名密码（BCrypt 比对）
3. 验证账号状态（是否冻结）
4. 生成 JWT Token（包含 userId、username、role）
5. 返回 Token 给前端

**授权流程：**
1. 定义不需要认证的公开接口白名单
2. 添加 JWT 拦截器，从 Header 提取 Token
3. 验证 Token 有效性
4. 解析 Token 存入 ThreadLocal 或 Request Context
5. 角色权限判断（客户/管理员）

**白名单接口：**
| 接口路径 | 说明 |
|----------|------|
| /auth/login | 登录 |
| /auth/register | 注册 |
| /auth/captcha | 验证码（可选） |
| /vehicles/public | 公开车辆查询 |

### 3.3 接口设计

| 接口 | 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|------|
| 登录 | POST | /auth/login | 用户登录 | 公开 |
| 登出 | POST | /auth/logout | 用户登出 | 已认证 |
| 刷新Token | POST | /auth/refresh | 刷新Token | 已认证 |
| 获取用户信息 | GET | /auth/info | 获取当前登录用户 | 已认证 |

### 3.4 核心数据对象

- **LoginRequest**：username、password
- **LoginResponse**：token、userId、username、realName、role、creditScore
- **UserContext**：当前登录用户上下文（userId、username、role）

---

## 四、用户管理模块

### 4.1 模块职责

- 客户注册
- 客户信息查询与修改
- 信用分查询与更新
- 账号状态管理

### 4.2 开发策略

**注册策略：**
- 注册时只保存基本信息（用户名、密码、驾照号、真实姓名）
- 密码使用 BCrypt 加密存储
- 驾照号必须唯一
- 新注册账号默认信用分 100
- 注册后账号状态为"待审核"或直接"正常"（按需求）
- 工作人员可审核通过账号

**信用分策略：**
- 初始信用分：100
- 信用分范围：0-100
- 扣分规则：超时还车扣5分、事故扣分由管理员填写
- 信用分低于60分时无法创建新预订
- 信用分更新后检查是否触发冻结

**接口设计：**

| 接口 | 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|------|
| 注册 | POST | /users/register | 客户注册 | 公开 |
| 查询用户信息 | GET | /users/{id} | 查询用户详情 | 本人或管理员 |
| 更新用户信息 | PUT | /users/{id} | 更新用户信息 | 本人或管理员 |
| 查询信用分 | GET | /users/{id}/credit | 查询信用分 | 本人或管理员 |
| 用户列表 | GET | /users | 分页查询用户 | 管理员 |
| 审核用户 | PUT | /users/{id}/approve | 审核通过用户 | 管理员 |
| 冻结用户 | PUT | /users/{id}/suspend | 冻结用户账号 | 管理员 |

### 4.3 业务规则

| 规则 | 说明 |
|------|------|
| 驾照号唯一 | 同一个驾照号只能注册一个账号 |
| 信用分下限 | 扣分不能低于0，公式：new = max(0, old - points) |
| 冻结检测 | 信用分低于60时自动冻结，信用分恢复后自动解冻 |
| 租车资格 | 账号状态正常、信用分≥60、驾照号不为空 |

---

## 五、车辆管理模块

### 5.1 模块职责

- 车辆信息增删改查
- 车辆状态管理
- 车辆配置管理（类型、价格、费率）
- 车辆图片管理（可选）

### 5.2 开发策略

**车辆状态流转：**
- idle（空闲）：可被预订
- reserved（已预约）：客户已预订，待确认
- confirmed（已预留）：工作人员已确认
- rented（已租用）：客户已取车
- maintenance（维修中）：事故维修中

**车辆状态转换规则：**
| 当前状态 | 可转换到 |
|----------|----------|
| idle | reserved, maintenance |
| reserved | confirmed, idle |
| confirmed | rented, idle |
| rented | idle, maintenance |
| maintenance | idle |

**配置管理策略：**
- 车辆类型固定：economy（经济型）、luxury（豪华型）
- 配置数据作为字典表，初始化时插入
- 车辆与配置通过 vehicle_type 关联或 config_id 外键关联

**接口设计：**

| 接口 | 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|------|
| 车辆列表 | GET | /vehicles | 分页+筛选查询 | 已认证 |
| 车辆详情 | GET | /vehicles/{id} | 查询车辆详情 | 已认证 |
| 新增车辆 | POST | /vehicles | 新增车辆 | 管理员 |
| 更新车辆 | PUT | /vehicles/{id} | 更新车辆信息 | 管理员 |
| 删除车辆 | DELETE | /vehicles/{id} | 删除车辆 | 管理员 |
| 更新状态 | PUT | /vehicles/{id}/status | 更新车辆状态 | 管理员 |
| 配置列表 | GET | /vehicle-configs | 查询配置列表 | 管理员 |
| 更新配置 | PUT | /vehicle-configs/{id} | 更新费率配置 | 管理员 |

**查询筛选条件：**
- 车辆状态（支持多选）
- 车辆类型（经济型/豪华型）
- 品牌/车型关键字模糊搜索
- 价格区间筛选

### 5.3 业务规则

| 规则 | 说明 |
|------|------|
| 车牌号唯一 | 同一车牌号不能重复录入 |
| 删除限制 | 有进行中预订的车辆不能删除 |
| 状态联动 | 维修状态时不允许被预订 |

---

## 六、预订管理模块（核心）

### 6.1 模块职责

- 创建预订申请
- **时间冲突检测（评分重点）**
- 预订确认/取消
- 预订列表查询

### 6.2 开发策略

**预订创建流程：**
1. 接收预订请求（车辆ID、取车时间、还车时间）
2. 计算计划租用天数（还车日期 - 取车日期）
3. 执行前置校验（6项检查）
4. 调用时间冲突检测
5. 生成预订编号（规则：BK + yyyyMMdd + 6位随机数）
6. 计算预计租金快照（天数 × 日租金）
7. 保存预订记录，状态为 pending
8. 更新车辆状态为 reserved
9. 返回预订信息

**前置校验（6项）：**
| 序号 | 检查项 | 校验逻辑 |
|------|--------|----------|
| 1 | 车辆存在性 | 车辆ID存在且未被逻辑删除 |
| 2 | 车辆可用性 | 车辆状态必须是 idle |
| 3 | 时间有效性 | 取车时间 >= 今天，还车时间 > 取车时间 |
| 4 | 时间冲突 | 该时间段内无其他有效预订 |
| 5 | 用户重复预订 | 同一用户无 pending/confirmed/picked_up 状态的预订 |
| 6 | 账号有效性 | 账号状态 normal，信用分 ≥ 60，驾照号不为空 |

**时间冲突检测算法（评分重点）：**
- 输入：新预订时间段 [newStart, newEnd]，已有预订时间段 [existStart, existEnd]
- 判断公式：newStart ≤ existEnd AND existStart ≤ newEnd 时为冲突
- 等号算冲突：因为需要留出清洁和交接时间
- 需要检查的预订状态：pending、confirmed、picked_up（进行中的预订）

**预订确认流程：**
1. 验证预订状态为 pending
2. 验证车辆状态为 reserved
3. 更新预订状态为 confirmed
4. 更新车辆状态为 confirmed

**预订取消流程：**
1. 验证预订状态为 pending 或 confirmed
2. 更新预订状态为 cancelled
3. 恢复车辆状态为 idle

**接口设计：**

| 接口 | 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|------|
| 创建预订 | POST | /bookings | 客户提交预订 | 客户 |
| 确认预订 | PUT | /bookings/{id}/confirm | 工作人员确认 | 管理员 |
| 取消预订 | PUT | /bookings/{id}/cancel | 取消预订 | 客户/管理员 |
| 我的预订 | GET | /bookings/my | 查询我的预订列表 | 客户 |
| 所有预订 | GET | /bookings | 查询所有预订（管理） | 管理员 |
| 预订详情 | GET | /bookings/{id} | 查询预订详情 | 本人/管理员 |

**预订状态流转：**
```
pending（待确认）─→ confirmed（已确认）─→ picked_up（已取车）─→ returned（已还车）
     │                    │                    │
     └──→ cancelled       └──→ cancelled       └──→ overdue（超时）
```

### 6.3 业务规则汇总

| 规则 | 实现要点 |
|------|----------|
| 等号算冲突 | 使用 ≤ 和 ≥ 比较，而非 < 和 > |
| 无重复预订 | 同一用户只能有一个进行中的预订 |
| 信用分门槛 | 信用分低于60分无法预订 |
| 用车最小天数 | 最小租用1天（取车当天不能还车） |
| 提前预订限制 | 最长可提前30天预订（可配置） |

---

## 七、取还车模块（核心）

### 7.1 模块职责

- 取车处理
- 还车处理
- **超时小时向上取整计算**
- **费用计算（租金+超时费+超里程费）**
- **信用分扣减（最低0分）**
- 费用明细查询

### 7.2 开发策略

**取车流程：**
1. 验证预订状态为 confirmed
2. 验证车辆状态为 confirmed 或 reserved
3. 记录实际取车时间（服务器当前时间）
4. 记录取车里程（工作人员输入）
5. 计算计划还车时间 = 取车时间 + 计划天数
6. 更新预订状态为 picked_up
7. 更新车辆状态为 rented

**还车流程（核心事务）：**
1. 验证预订状态为 picked_up 或 overdue
2. 验证还车里程 ≥ 取车里程
3. 记录实际还车时间（服务器当前时间）
4. 执行费用计算（调用 FeeCalculator）
5. 执行信用分扣减（如有超时）
6. 在同一个数据库事务中更新：
   - 预订记录：写入所有费用快照、更新状态为 returned
   - 车辆信息：更新当前里程、状态为 idle
   - 用户信息：更新信用分

**超时检测（定时任务）：**
- 定期扫描状态为 picked_up 且计划还车时间 < 当前时间的预订
- 将这些预订状态更新为 overdue
- 超时检测和还车时的扣分是独立的

**费用计算策略（评分重点）：**

**1. 预计租金：**
- 已在创建预订时计算快照
- 计划天数 × 日租金

**2. 超时费计算：**
- 计算超时分钟数 = 实际还车时间 - 计划还车时间
- 如果实际还车时间 ≤ 计划还车时间，超时费 = 0
- 超时小时数 = ceil(超时分钟数 / 60) 【向上取整】
- 超时费 = 超时小时数 × 每小时超时费率

**3. 超里程费计算：**
- 实际行驶里程 = 还车里程 - 取车里程
- 总免费里程额度 = 计划天数 × 每天免费里程
- 超里程数 = max(0, 实际行驶里程 - 总免费里程额度)
- 超里程费 = 超里程数 × 每公里费率

**4. 总费用 = 预计租金 + 超时费 + 超里程费**

**信用分扣减策略：**
- 超时还车固定扣5分
- 与超时时长无关（超5分钟和超5小时都扣5分）
- 扣分公式：新信用分 = max(0, 原信用分 - 5)
- 信用分变化记录到预订表

**接口设计：**

| 接口 | 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|------|
| 取车 | POST | /rental/pickup | 办理取车 | 管理员 |
| 还车 | POST | /rental/return | 办理还车 | 管理员 |
| 费用详情 | GET | /rental/fee/{bookingId} | 查询费用明细 | 本人/管理员 |
| 超时检测 | POST | /rental/check-overdue | 手动触发超时检测 | 管理员 |

### 7.3 业务规则汇总

| 规则 | 实现要点 |
|------|----------|
| 超时向上取整 | 必须使用 Math.ceil()，不能用 Math.round() |
| 免费里程叠加 | 总额 = 天数 × 每天免费里程，不是每天单独计算 |
| 信用分不低于0 | 使用 max(0, score - deduct) |
| 超时固定扣5分 | 与超时时长无关 |
| 还车里程校验 | 还车里程 ≥ 取车里程，否则提示错误 |
| 费用系统计算 | 费用不允许手工输入，全部由系统自动计算 |

---

## 八、事故管理模块

### 8.1 模块职责

- 事故记录创建
- 事故处理（扣分、维修计划）
- 维修完成处理
- 车辆状态联动

### 8.2 开发策略

**事故登记流程：**
1. 关联预订ID（还车时或还车后登记）
2. 记录事故描述、事故时间
3. 创建事故记录，状态为 pending
4. 可选：立即将车辆状态改为 maintenance

**事故处理流程：**
1. 管理员录入扣分值
2. 管理员填写预计维修完成日期
3. 更新事故状态为 in_progress
4. 执行信用分扣减（使用同一扣分规则：max(0, 原分 - 扣分值)）

**维修完成流程：**
1. 填写实际完成日期
2. 更新事故状态为 completed
3. 检查车辆是否还有其他进行中的事故
4. 如无其他事故，将车辆状态恢复为 idle

**接口设计：**

| 接口 | 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|------|
| 创建事故 | POST | /accidents | 创建事故记录 | 管理员 |
| 事故列表 | GET | /accidents | 分页查询事故 | 管理员 |
| 车辆事故 | GET | /accidents/vehicle/{vehicleId} | 查询车辆事故 | 管理员 |
| 事故详情 | GET | /accidents/{id} | 查询事故详情 | 管理员 |
| 处理事故 | PUT | /accidents/{id}/process | 录入扣分和维修计划 | 管理员 |
| 维修完成 | PUT | /accidents/{id}/complete | 维修完成 | 管理员 |

### 8.3 业务规则

| 规则 | 说明 |
|------|------|
| 扣分不超下限 | 扣分后信用分最低0分 |
| 车辆状态联动 | 维修中车辆不能被预订 |
| 多事故处理 | 同一车辆多个事故时，所有完成后才能恢复空闲 |
| 事故关联预订 | 事故必须关联到具体预订 |

---

## 九、接口设计规范

### 9.1 RESTful 规范

**URL命名规则：**
- 使用名词复数形式
- 小写字母，单词间用连字符分隔
- 示例：/users、/vehicles、/bookings、/rental-orders

**HTTP方法使用：**
| 方法 | 用途 | 示例 |
|------|------|------|
| GET | 查询资源 | GET /vehicles/{id} |
| POST | 创建资源 | POST /bookings |
| PUT | 全量更新 | PUT /users/{id} |
| PATCH | 部分更新 | PATCH /bookings/{id}/status |
| DELETE | 删除资源 | DELETE /vehicles/{id} |

**状态码使用：**
| 状态码 | 说明 | 使用场景 |
|--------|------|----------|
| 200 | 成功 | 正常响应 |
| 201 | 创建成功 | POST 请求成功后 |
| 204 | 无内容 | DELETE 成功 |
| 400 | 参数错误 | 参数校验失败 |
| 401 | 未认证 | Token无效或过期 |
| 403 | 无权限 | 角色权限不足 |
| 404 | 资源不存在 | 查询的数据不存在 |
| 409 | 冲突 | 时间冲突等业务冲突 |
| 422 | 业务规则失败 | 信用分不足等 |
| 500 | 服务器错误 | 未知异常 |

### 9.2 请求参数规范

**路径参数：**
- 用于资源ID：`/vehicles/{id}`
- 使用整数或UUID

**查询参数：**
- 分页：`page`（页码，从1开始）、`size`（每页条数）
- 排序：`sort`（排序字段）、`order`（asc/desc）
- 筛选：`status`、`type` 等字段名直接作为参数

**请求体：**
- JSON 格式
- Content-Type: application/json
- 字段名使用小驼峰命名

### 9.3 响应规范

**成功响应格式：**
```json
{
  "code": 200,
  "message": "success",
  "data": { ... },
  "timestamp": 1234567890
}
```

**分页响应格式：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 100,
    "page": 1,
    "size": 10,
    "pages": 10,
    "records": [ ... ]
  },
  "timestamp": 1234567890
}
```

**错误响应格式：**
```json
{
  "code": 422,
  "message": "信用分低于60分，无法租车，当前信用分：55",
  "data": null,
  "timestamp": 1234567890
}
```

### 9.4 认证规范

**Token传递方式：**
- Header 名称：`Authorization`
- Header 值格式：`Bearer <token>`

**示例：**
```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
```

### 9.5 日期时间格式规范

| 场景 | 格式 | 示例 |
|------|------|------|
| 请求中的日期时间 | yyyy-MM-dd HH:mm:ss | 2024-03-08 09:00:00 |
| 响应中的日期时间 | yyyy-MM-dd HH:mm:ss | 2024-03-08 09:00:00 |
| 仅日期 | yyyy-MM-dd | 2024-03-08 |

**时区约定：**
- 统一使用 Asia/Shanghai（北京时间）

---

## 十、开发优先级

### 第一阶段（MVP核心功能）
1. 项目初始化和基础配置
2. 数据库表创建
3. 统一响应和异常处理
4. JWT认证登录
5. 车辆列表查询
6. 创建预订 + 时间冲突检测
7. 预订确认
8. 取车
9. 还车 + 费用计算
10. 我的预订查询

### 第二阶段（完善业务）
1. 用户注册
2. 用户信用分管理
3. 车辆管理（增删改）
4. 预订取消
5. 费用明细展示
6. 超时检测定时任务

### 第三阶段（扩展功能）
1. 事故与维修管理
2. 管理员预订列表
3. 车辆状态筛选
4. 统计看板（可选）

---

## 十一、测试策略

### 11.1 单元测试重点

| 模块 | 测试重点 | 测试用例 |
|------|----------|----------|
| FeeCalculator | 超时小时向上取整 | 5分钟→1小时，61分钟→2小时，120分钟→2小时 |
| TimeConflictValidator | 等号算冲突 | 周四-周六 vs 周六-周一 → 冲突 |
| CreditScoreValidator | 信用分下限 | 15分扣20分 → 0分 |
| MileageCalculator | 免费里程叠加 | 3天×200公里=600公里总额 |

### 11.2 集成测试重点

- 完整预订→确认→取车→还车流程
- 并发预订同车的冲突处理
- 信用分不足时的预订拒绝
- 超时还车的扣分和费用计算

---

## 十二、注意事项汇总

### 评分关键点（必须正确实现）

1. **超时小时向上取整**：必须使用 `Math.ceil()`
2. **时间冲突等号算冲突**：比较条件包含等号
3. **免费里程按天数叠加**：总额 = 天数 × 每天免费里程
4. **信用分最低为0**：使用 `Math.max(0, newScore)`

### 技术注意事项

1. 数据库时间字段统一使用 DATETIME
2. 金额字段使用 DECIMAL(10,2)
3. 密码必须 BCrypt 加密
4. 所有数据库写操作考虑事务
5. 避免 N+1 查询问题
6. 接口做参数校验（@Valid）
7. CORS 配置允许前端端口访问

### 业务注意事项

1. 还车里程必须 ≥ 取车里程
2. 费用全部由系统计算，不接受人工输入
3. 超时还车固定扣5分，与超时时长无关
4. 同一用户同一时间只能有一个进行中的预订
5. 车辆维修期间不能创建新预订